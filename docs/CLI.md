# CLI

The `pmh` CLI will emit a versioned JSON envelope containing command identity, current state, diagnostics, effects, artifacts, and allowed next actions.

Initial surfaces cover venue inspection, claim/link inspection, opportunity verification, deterministic replay, shadow execution, campaign inspection, and Core projections. Human-readable Studio views consume the same projections and never recompute verdicts.
